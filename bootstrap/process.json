{
  "apps": [
    {
      "name": "odd.crawler",
      "script": "../crawler/crawler.js",
      "watch": ["../crawler"],
      "ignore_watch": ["../crawler/test"],
      "watch_options": {
        "usePolling": true
      }
    },
    {
      "name": "odd.server",
      "script": "../server/server.js",
      "watch": "../server",
      "ignore_watch": ["../server/test"],
      "watch_options": {
        "usePolling": true
      },
      "env": {
        "NODE_ENV": "development"
      },
      "env_production": {
        "NODE_ENV": "production"
      }
    },
    {
      "name": "odd.tile-server",
      "script": "../tile-server/server.js",
      "watch": ["../tile-server"],
      "ignore_watch": ["../tile-server/test"],
      "watch_options": {
        "usePolling": true
      }
    }
  ]
}
